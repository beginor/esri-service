{
  "name": "esri-service",
  "version": "1.0.1",
  "description": "Common used methods helper for ArcGIS JS API",
  "main": "build/index.js",
  "typings": "build/index.d.ts",
  "scripts": {
    "publish": "rm -rf build && npm run build && cp src/package.json build/package.json && npm publish build",
    "build": "tsc -p tsconfig.json",
    "test": "test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/beginor/esri-service.git"
  },
  "author": "beginor",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/beginor/esri-service/issues"
  },
  "homepage": "https://github.com/beginor/esri-service#readme",
  "devDependencies": {
    "@types/arcgis-js-api": "^4.10.0",
    "tslint": "^5.12.0",
    "typescript": "^3.2.0"
  },
  "peerDependencies": {
    "esri-loader": "^2.6.0",
    "tslib": "^1.9.0"
  }
}
